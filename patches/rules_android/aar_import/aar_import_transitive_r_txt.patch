--- a/rules/resources.bzl
+++ b/rules/resources.bzl
@@ -1607,21 +1607,29 @@
         out_package_aware_r_txt = ctx.actions.declare_file(
             ctx.label.name + "_symbols/package_aware_R.txt",
         ) if not _should_link else None
+
+        # aar_import targets need transitive resource deps in merge_compiled
+        # so their r_txt includes resources from dependencies.  Pre-compiled
+        # AAR bytecode (built with nonTransitiveRClasses=false) references
+        # transitive R fields at runtime that must appear in the library R
+        # class generated by generate_binary_r in the binary.
+        _merge_transitive = _should_link or hasattr(ctx.attr, "aar")
+
         _busybox.merge_compiled(
             ctx,
             out_class_jar = out_class_jar,
             out_manifest = processed_manifest,
             out_aapt2_r_txt = out_aapt2_r_txt,
             out_package_aware_r_txt = out_package_aware_r_txt,
             java_package = java_package,
             manifest = manifest,
             compiled_resources = compiled_resources,
             direct_resources_nodes = depset(
-                transitive = direct_resources_nodes if _should_link else [],
+                transitive = direct_resources_nodes if _merge_transitive else [],
                 order = "postorder",
             ),
             transitive_resources_nodes = depset(
-                transitive = transitive_resources_nodes if _should_link else [],
+                transitive = transitive_resources_nodes if _merge_transitive else [],
                 order = "postorder",
             ),
             direct_compiled_resources = depset(
-                transitive = direct_compiled_resources if _should_link else [],
+                transitive = direct_compiled_resources if _merge_transitive else [],
                 order = "postorder",
             ),
             transitive_compiled_resources = depset(
-                transitive = transitive_compiled_resources if _should_link else [],
+                transitive = transitive_compiled_resources if _merge_transitive else [],
                 order = "postorder",
             ),
             android_jar = android_jar,
