diff --git a/MODULE.bazel b/MODULE.bazel
index 474f98411..15dc4a8d8 100644
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -157,7 +157,7 @@ maven.install(
     ],
     # To generate, run:
     # REPIN=1 bazelisk run --enable_bzlmod @rules_android_maven//:pin
-    # lock_file = "//:rules_android_maven_install.json",
+    lock_file = "//:rules_android_maven_install.json",
     use_starlark_android_rules = True,
 )
 use_repo(
diff --git a/defs.bzl b/defs.bzl
index 3e50cb67c..24ca08908 100644
--- a/defs.bzl
+++ b/defs.bzl
@@ -139,8 +139,8 @@ def rules_android_workspace():
         aar_import_bzl_label = "@rules_android//rules:rules.bzl",
         # To generate:
         # REPIN=1 bazelisk run --noenable_bzlmod @unpinned_rules_android_maven//:pin
-        # maven_install_json = "//:rules_android_maven_install.json",
-        # NOTE: above lockfile currently disabled due to https://github.com/bazelbuild/rules_jvm_external/issues/1134.
+        # Upstream leaves this disabled due to https://github.com/bazelbuild/rules_jvm_external/issues/1134.
+        maven_install_json = "@rules_android//:rules_android_maven_install.json",
     )

     maven_install(
